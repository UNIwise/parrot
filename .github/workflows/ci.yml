name: CI

on:
  workflow_dispatch: {}
  pull_request: {}
  release:
    types: [published]
  push:
    branches:
      - master
      - develop
      - release/*
      - feature/*
      - hotfix/*

jobs:
  golang:
    uses: UNIwise/workflows/.github/workflows/golang.yaml@master
    secrets: inherit
    with:
      go-version: 1.19
      golangci-lint-version: v1.61.0
