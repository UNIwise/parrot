name: CI

on:
  workflow_dispatch: {}
  pull_request: {}
  release:
    types: [published]
  push:
    branches:
      - master

jobs:
  golang:
    uses: UNIwise/workflows/.github/workflows/golang.yaml@master
    secrets: inherit
    with:
      go-version: 1.26
      golangci-lint-version: v2.11.4